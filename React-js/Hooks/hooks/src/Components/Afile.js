import React from 'react';
import Bfile from './Bfile';
const Afile = (prop) => {
    return (
        <div>
            
            <Bfile name={prop.name}/>
        </div>
    );
}

export default Afile;
